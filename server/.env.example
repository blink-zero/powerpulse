# PowerPulse Server Environment Variables
# Copy this file to .env and update the values as needed

# =============================================
# REQUIRED VARIABLES
# =============================================

# Server Configuration (REQUIRED)
PORT=5000
NODE_ENV=development

# JWT Secret for Authentication (REQUIRED)
# IMPORTANT: Change this to a secure random string in production
# You can generate a secure random string with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here

# =============================================
# NUT SERVER CONFIGURATION
# =============================================

# Default NUT Server Host (REQUIRED)
# If your NUT server is running on the host machine and you're using Docker, use host.docker.internal
DEFAULT_NUT_HOST=localhost

# Default NUT Server Port (REQUIRED)
DEFAULT_NUT_PORT=3493

# Optional: NUT Server Authentication
# Uncomment and set these if your NUT server requires authentication
# DEFAULT_NUT_USERNAME=
# DEFAULT_NUT_PASSWORD=

# =============================================
# DATABASE CONFIGURATION
# =============================================

# Database Path (Optional)
# By default, the database is stored in ./data/database.sqlite
# DB_PATH=/custom/path/to/database.sqlite

# =============================================
# SECURITY CONFIGURATION
# =============================================

# HTTPS Configuration (Recommended for production)
# Set to true to enable HTTPS
# ENABLE_HTTPS=true

# Paths to SSL certificate and key files (Required if HTTPS is enabled)
# SSL_CERT_PATH=/path/to/certificate.pem
# SSL_KEY_PATH=/path/to/key.pem

# =============================================
# LOGGING CONFIGURATION
# =============================================

# Log Level (Optional)
# Valid values: error, warn, info, debug
# LOG_LEVEL=info

# =============================================
# EMAIL CONFIGURATION
# =============================================

# SMTP Server Settings (Optional)
# If not provided, test emails will be sent to Ethereal Email for development
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-username
# SMTP_PASS=your-password
# SMTP_FROM=powerpulse@example.com

# =============================================
# ADVANCED CONFIGURATION
# =============================================

# NUT Client Connection Timeout (Optional)
# Timeout in milliseconds for NUT client connections
# NUT_CONNECTION_TIMEOUT=5000

# NUT Client Retry Attempts (Optional)
# Number of retry attempts for NUT client connections
# NUT_RETRY_ATTEMPTS=3

# NUT Client Retry Delay (Optional)
# Delay in milliseconds between retry attempts
# NUT_RETRY_DELAY=1000
